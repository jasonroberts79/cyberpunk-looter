# Project settings
- Always use uv to manage dependencies and virtual environments
- Don't use Makefiles
- When writing code, always use the command 'uvx ty check' to do static type checking and run the tests via 'uv run pytest'

# Modules
- Use @.claude/RIPER-5.md
